# Run lint-staged: format -> stage -> lint (fails on errors)
npx lint-staged

# Check CSS file line counts
npm run check:css

# Format Rust code
npm run fmt:rust

# Check for Rust errors with clippy
# This will fail the commit if there are any clippy warnings
npm run lint:rust
