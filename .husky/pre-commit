# Format the entire project
npm run format

# Stage all formatted files
git add -A

# Check for ESLint errors in the entire project
npm run lint

# Run lint-staged for additional checks if needed
npx lint-staged